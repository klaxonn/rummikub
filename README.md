Simple implémentation du jeu Rummikub.

Pour l'instant uniquement jouable en ligne de commande partagée par tous les joueurs.

Pour compiler : gradle build
Pour lancer le jeu : java -jar build/libs/Rummikub.jar
